# Per-project status JSONs

This folder is written by the daily Clari digest automation. Each `<slug>.json` holds the latest synthesized status for one account in the `In Progress` tab of *Connected CMMS Project Status*. `index.json` lists all slugs with a summary row.

The dashboard's **Project Latest Update** tab fetches these files at runtime.
